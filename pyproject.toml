[tool.black]
line-length = 88
skip-string-normalization = 1

[tool.ruff]
line-length = 88
select = ["F", "E", "W", "I001"]
ignore = ["E501"]  # Skip line length violations
src = ["app_api", "app_bot", "app_worker"]
